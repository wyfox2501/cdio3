import React from 'react';
import PropTypes from 'prop-types';

home.propTypes = {
    
};

function home(props) {
    return (
        <div>
            
        </div>
    );
}

export default home;